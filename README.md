# Skeleton Loading UI

A simple skeleton loading implementation built with Next.js and TypeScript.

This project demonstrates how skeletons can be used to improve the loading experience while waiting for API data.
It also has a throttle as a 3 second delay so u can see the skeleton on each fetch or refresh.
